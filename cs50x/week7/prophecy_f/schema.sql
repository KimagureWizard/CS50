CREATE TABLE names (
    name TEXT,
    PRIMARY KEY(name)
);

CREATE TABLE houses (
    house TEXT,
    PRIMARY KEY(house)
);

CREATE TABLE assignment (
    name TEXT,
    house TEXT,
    PRIMARY KEY(name)
);